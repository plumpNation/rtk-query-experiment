RTK query experiment
====================

## Up and running

- `pnpm install`
- `pnpm run dev`
- In another terminal window `pnpm run mock:db`

Uses a local JSON server, proxied by Vite, to provide API.